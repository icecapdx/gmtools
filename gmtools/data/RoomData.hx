package gmtools.data;

typedef RoomData = {
    var name:String;
    var width:Int;
    var height:Int;
    var speed:Int; // fps
    var background:String;
    var playerX:Int;
    var playerY:Int;
    var hasPlayer:Bool;
}